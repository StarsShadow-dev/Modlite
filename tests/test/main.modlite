import { print startsWith error } from "../../StandardLibrary.json"

var msg string

public function main() void {
	msg = "Hello, World"
	print("in main")
}

test print {
	print(msg)
}

test startsWith_true {
	if (startsWith(msg "Hello") == true) {
		print("PASS")
	} else {
		error("test fail")
	}
}
test startsWith_true {
	if (startsWith(msg "bye") != true) {
		print("PASS")
	} else {
		error("test fail")
	}
}